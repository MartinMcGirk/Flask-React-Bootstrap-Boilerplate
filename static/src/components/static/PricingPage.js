import React from 'react'
import { Link } from 'react-router-dom';

const PricingPage = (props) => (
    <div className="container-fluid">
        This will be the pricing page
    </div>
);

export { PricingPage }